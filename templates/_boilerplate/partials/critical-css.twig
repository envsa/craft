{% import '_boilerplate/partials/cookies' as cookies %}

{% if cookies.get('MAIN_CSS') %}
  {# {{ craft.twigpack.includeCssModule('main.css', false) }} #}
{% else %}
  {% do cookies.set('MAIN_CSS', '1', '+7 Days', '/', '', true, true) %}

  <!-- Critical css -->
  {# {{
    craft.twigpack.includeInlineCssTags(
      '@webroot/dist/' ~ (entry.type.name|lower) ~ '_critical.min.css'
    )
  }} #}

  {# {{ craft.twigpack.includeCssModule('main.css', true) }} #}
{% endif %}
