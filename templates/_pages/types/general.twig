{% extends '_layouts/generic-page-layout' %}
{% set ctas = entry.ctas.all() %}

{% block content %}
	<div class="bg-white grid lg:grid-cols-5 px-5 pt-8 space-y-4 lg:space-y-0">
		<div class="col-span-3">
			{% include "_partials/nav-side" %}
		</div>
		{# <div class="lg:col-span-3 text-gray-900 leading-normal space-y-6 px-8">
							{% include "_partials/breadcrumbs" %}
							{% include "_partials/hero" %}
							<h1>
								{{ entry.title }}
							</h1>

							{% if entry.contentBuilder %}
								{% set contentBuilder = entry.contentBuilder.level(1).all() %}
								{% include '_partials/content-builder' with {
				          blocks: contentBuilder
				        } %}
							{% endif %}

						</div>
						<div class="col-span-1 flex flex-col">
							{% if ctas %}
								{% include "_partials/cta" %}
							{% endif %}

							{% include "_partials/recent-posts" %}
						</div> #}
	</div>
{% endblock %}
